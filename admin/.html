<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.5, user-scalable=yes">
    <title>SIWES & Industrial Training Tracker · British Poly Admin</title>
    <!-- Google Font + minimal reset -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600;14..32,700;14..32,800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: #f6faff;
            color: #0a1e3c;
            min-height: 100vh;
            display: flex;
        }

        /* ===== SIDEBAR ===== */
        .sidebar {
            width: 260px;
            background: #0a1e3c;
            color: white;
            min-height: 100vh;
            padding: 1.8rem 1.2rem 2rem;
            display: flex;
            flex-direction: column;
            flex-shrink: 0;
            position: sticky;
            top: 0;
            height: 100vh;
            overflow-y: auto;
            transition: transform 0.25s ease;
            z-index: 200;
        }

        .sidebar .logo {
            font-weight: 800;
            font-size: 1.4rem;
            letter-spacing: -0.02em;
            margin-bottom: 2rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }

        .sidebar .logo span {
            color: #6a9aff;
        }

        .sidebar .logo small {
            font-weight: 400;
            font-size: 0.6rem;
            color: rgba(255, 255, 255, 0.4);
            display: block;
            margin-top: 0.1rem;
        }

        .sidebar .nav-section {
            margin-bottom: 1.5rem;
        }

        .sidebar .nav-section .label {
            font-size: 0.6rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: rgba(255, 255, 255, 0.3);
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .sidebar .nav-item {
            display: flex;
            align-items: center;
            gap: 0.8rem;
            padding: 0.6rem 0.8rem;
            border-radius: 12px;
            color: rgba(255, 255, 255, 0.6);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.85rem;
            transition: 0.15s ease;
            margin-bottom: 0.15rem;
            cursor: pointer;
        }

        .sidebar .nav-item:hover {
            background: rgba(255, 255, 255, 0.08);
            color: white;
        }

        .sidebar .nav-item.active {
            background: rgba(255, 255, 255, 0.12);
            color: white;
        }

        .sidebar .nav-item .icon {
            font-size: 1.1rem;
            width: 28px;
            text-align: center;
        }

        .sidebar .nav-item .badge {
            margin-left: auto;
            background: #1a5cff;
            color: white;
            font-size: 0.6rem;
            padding: 0.05rem 0.6rem;
            border-radius: 40px;
            font-weight: 600;
        }

        .sidebar .user-card {
            background: rgba(255, 255, 255, 0.06);
            border-radius: 16px;
            padding: 0.8rem 1rem;
            margin-top: auto;
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .sidebar .user-card .avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: #1a5cff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.9rem;
            color: white;
            flex-shrink: 0;
        }

        .sidebar .user-card .info {
            font-size: 0.8rem;
        }

        .sidebar .user-card .info .name {
            font-weight: 600;
            color: white;
        }

        .sidebar .user-card .info .role {
            font-size: 0.65rem;
            color: rgba(255, 255, 255, 0.5);
        }

        /* ===== MAIN CONTENT ===== */
        .main-content {
            flex: 1;
            padding: 1.5rem 2rem 3rem;
            max-width: calc(100% - 260px);
            width: 100%;
        }

        /* ===== TOP BAR ===== */
        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 0.8rem;
            background: white;
            padding: 0.7rem 1.5rem;
            border-radius: 60px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
            border: 1px solid #eef2f8;
            margin-bottom: 2rem;
        }

        .top-bar .greeting {
            font-weight: 600;
            font-size: 1rem;
        }

        .top-bar .greeting small {
            font-weight: 400;
            color: #4a6a8a;
            font-size: 0.8rem;
        }

        .top-bar .actions {
            display: flex;
            gap: 0.8rem;
            align-items: center;
            flex-wrap: wrap;
        }

        .top-bar .actions .btn {
            padding: 0.4rem 1rem;
            border-radius: 40px;
            font-weight: 600;
            font-size: 0.8rem;
            border: 2px solid transparent;
            cursor: pointer;
            transition: 0.15s ease;
            background: white;
            color: #0a1e3c;
            border-color: #c5d4ee;
            text-decoration: none;
        }

        .top-bar .actions .btn-primary {
            background: #0a1e3c;
            color: white;
            border-color: #0a1e3c;
        }

        .top-bar .actions .btn-primary:hover {
            background: #17335c;
            border-color: #17335c;
        }

        .top-bar .actions .btn-success {
            background: #0e7b3a;
            color: white;
            border-color: #0e7b3a;
        }

        .top-bar .actions .btn-success:hover {
            background: #1a934a;
            border-color: #1a934a;
        }

        /* ===== STATS ===== */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: white;
            border-radius: 16px;
            padding: 1.2rem 1.5rem;
            box-shadow: 0 4px 16px rgba(0, 20, 50, 0.04);
            border: 1px solid rgba(26, 92, 255, 0.06);
            text-align: center;
        }

        .stat-card .number {
            font-size: 1.8rem;
            font-weight: 800;
            color: #0a1e3c;
        }

        .stat-card .label {
            font-size: 0.75rem;
            color: #4a6a8a;
            font-weight: 500;
        }

        /* ===== TABS ===== */
        .tabs {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            background: white;
            padding: 0.5rem;
            border-radius: 60px;
            box-shadow: 0 4px 16px rgba(0, 20, 50, 0.04);
            border: 1px solid rgba(26, 92, 255, 0.06);
        }

        .tab-btn {
            padding: 0.5rem 1.5rem;
            border-radius: 40px;
            border: none;
            background: transparent;
            font-weight: 600;
            font-size: 0.85rem;
            color: #4a6a8a;
            cursor: pointer;
            transition: all 0.15s ease;
        }

        .tab-btn:hover {
            background: #f0f4fe;
            color: #0a1e3c;
        }

        .tab-btn.active {
            background: #0a1e3c;
            color: white;
            box-shadow: 0 4px 12px rgba(10, 30, 60, 0.15);
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        /* ===== FILTER BAR ===== */
        .filter-bar {
            background: white;
            border-radius: 24px;
            padding: 1rem 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: 0 4px 16px rgba(0, 20, 50, 0.04);
            border: 1px solid rgba(26, 92, 255, 0.06);
            display: flex;
            flex-wrap: wrap;
            gap: 0.8rem 1.5rem;
            align-items: center;
        }

        .filter-bar .search-group {
            flex: 2;
            min-width: 180px;
            display: flex;
            gap: 0.5rem;
            align-items: center;
        }

        .filter-bar .search-group input {
            flex: 1;
            padding: 0.5rem 1rem;
            border-radius: 60px;
            border: 2px solid #dce5f2;
            background: white;
            font-weight: 500;
            color: #0a1e3c;
            font-size: 0.85rem;
            outline: none;
            min-width: 100px;
        }

        .filter-bar .search-group input:focus {
            border-color: #1a5cff;
            box-shadow: 0 0 0 3px rgba(26, 92, 255, 0.1);
        }

        .filter-bar .filter-group {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem 1rem;
            align-items: center;
        }

        .filter-bar .filter-group label {
            font-weight: 500;
            font-size: 0.7rem;
            color: #1a3660;
            text-transform: uppercase;
            letter-spacing: 0.3px;
        }

        .filter-bar .filter-group select {
            padding: 0.3rem 1rem;
            border-radius: 60px;
            border: 2px solid #dce5f2;
            background: white;
            font-weight: 500;
            color: #0a1e3c;
            font-size: 0.8rem;
            outline: none;
            cursor: pointer;
        }

        .filter-bar .filter-group select:focus {
            border-color: #1a5cff;
            box-shadow: 0 0 0 3px rgba(26, 92, 255, 0.1);
        }

        .filter-bar .result-count {
            font-size: 0.8rem;
            color: #4a6a8a;
            margin-left: auto;
        }

        .filter-bar .result-count strong {
            color: #0a1e3c;
        }

        /* ===== TABLE ===== */
        .table-section {
            background: white;
            border-radius: 24px;
            padding: 0.5rem 0 1.5rem;
            box-shadow: 0 4px 16px rgba(0, 20, 50, 0.04);
            border: 1px solid rgba(26, 92, 255, 0.06);
            overflow-x: auto;
        }

        .table-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.6rem 1.5rem 0.3rem;
            flex-wrap: wrap;
            gap: 0.5rem;
        }

        .table-header .actions {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.4rem;
            padding: 0.5rem 1.2rem;
            border-radius: 60px;
            font-weight: 600;
            font-size: 0.8rem;
            border: 2px solid transparent;
            cursor: pointer;
            transition: all 0.15s ease;
            text-decoration: none;
        }

        .btn-sm {
            padding: 0.2rem 0.8rem;
            font-size: 0.7rem;
        }

        .btn-primary {
            background: #1a5cff;
            color: white;
            border-color: #1a5cff;
        }

        .btn-primary:hover {
            background: #2a6cff;
            border-color: #2a6cff;
        }

        .btn-success {
            background: #0e7b3a;
            color: white;
            border-color: #0e7b3a;
        }

        .btn-success:hover {
            background: #1a934a;
            border-color: #1a934a;
        }

        .btn-danger {
            background: #b22234;
            color: white;
            border-color: #b22234;
        }

        .btn-danger:hover {
            background: #a01e2e;
            border-color: #a01e2e;
        }

        .btn-warning {
            background: #d48c2c;
            color: white;
            border-color: #d48c2c;
        }

        .btn-warning:hover {
            background: #c07a1e;
            border-color: #c07a1e;
        }

        .btn-outline-blue {
            background: white;
            color: #0a1e3c;
            border-color: #0a1e3c;
        }

        .btn-outline-blue:hover {
            background: #edf4ff;
            border-color: #1a5cff;
            color: #1a5cff;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.85rem;
            min-width: 900px;
        }

        th {
            text-align: left;
            padding: 0.6rem 1rem 0.4rem;
            font-weight: 600;
            color: #1a3660;
            border-bottom: 2px solid #e6edf8;
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            white-space: nowrap;
            cursor: pointer;
        }

        td {
            padding: 0.5rem 1rem;
            border-bottom: 1px solid #ecf2fa;
            vertical-align: middle;
        }

        tr:last-child td {
            border-bottom: none;
        }

        tr:hover td {
            background: #fafcff;
        }

        .status-badge {
            padding: 0.1rem 0.7rem;
            border-radius: 40px;
            font-size: 0.6rem;
            font-weight: 600;
            text-transform: uppercase;
        }

        .status-active {
            background: #ddf0e6;
            color: #0e7b3a;
        }
        .status-pending {
            background: #f0ece6;
            color: #d48c2c;
        }
        .status-completed {
            background: #e6eeff;
            color: #1a5cff;
        }
        .status-suspended {
            background: #f0e6e6;
            color: #b22234;
        }
        .status-approved {
            background: #ddf0e6;
            color: #0e7b3a;
        }
        .status-review {
            background: #f0ece6;
            color: #d48c2c;
        }

        .action-btns {
            display: flex;
            gap: 0.3rem;
            flex-wrap: wrap;
            justify-content: flex-end;
        }

        /* ===== MODAL ===== */
        .modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(10, 30, 60, 0.45);
            backdrop-filter: blur(4px);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }

        .modal-overlay.open {
            display: flex;
        }

        .modal {
            background: white;
            max-width: 580px;
            width: 92%;
            max-height: 90vh;
            overflow-y: auto;
            padding: 2rem 2rem 2.2rem;
            border-radius: 32px;
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.2);
            animation: modalIn 0.2s ease;
        }

        @keyframes modalIn {
            0% {
                transform: translateY(20px);
                opacity: 0;
            }
            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .modal h2 {
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
        }

        .modal .form-group {
            margin-bottom: 1rem;
        }

        .modal .form-group label {
            display: block;
            font-weight: 600;
            font-size: 0.75rem;
            color: #1a3660;
            margin-bottom: 0.2rem;
            text-transform: uppercase;
            letter-spacing: 0.3px;
        }

        .modal .form-group label .required {
            color: #b22234;
        }

        .modal .form-group input,
        .modal .form-group select,
        .modal .form-group textarea {
            width: 100%;
            padding: 0.6rem 1rem;
            border-radius: 60px;
            border: 2px solid #dce5f2;
            background: white;
            font-weight: 500;
            color: #0a1e3c;
            font-size: 0.85rem;
            outline: none;
            font-family: inherit;
        }

        .modal .form-group textarea {
            border-radius: 20px;
            resize: vertical;
            min-height: 60px;
        }

        .modal .form-group input:focus,
        .modal .form-group select:focus,
        .modal .form-group textarea:focus {
            border-color: #1a5cff;
            box-shadow: 0 0 0 3px rgba(26, 92, 255, 0.1);
        }

        .modal .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        @media (max-width: 480px) {
            .modal .form-row {
                grid-template-columns: 1fr;
            }
        }

        .modal .modal-actions {
            display: flex;
            gap: 0.8rem;
            margin-top: 1.5rem;
            flex-wrap: wrap;
        }

        .modal .modal-actions .btn {
            flex: 1;
            justify-content: center;
            min-width: 100px;
        }

        /* ===== TOAST ===== */
        .toast {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            background: #0a1e3c;
            color: white;
            padding: 0.8rem 1.8rem;
            border-radius: 60px;
            font-weight: 500;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
            z-index: 2000;
            opacity: 0;
            transform: translateY(10px);
            transition: opacity 0.25s, transform 0.25s;
            pointer-events: none;
            font-size: 0.9rem;
        }

        .toast.show {
            opacity: 1;
            transform: translateY(0);
            pointer-events: auto;
        }

        .toast.success {
            background: #0e7b3a;
        }

        .toast.error {
            background: #b22234;
        }

        /* ===== RESPONSIVE ===== */
        .sidebar-toggle {
            display: none;
            position: fixed;
            top: 0.8rem;
            left: 0.8rem;
            z-index: 300;
            background: #0a1e3c;
            color: white;
            border: none;
            border-radius: 12px;
            padding: 0.6rem 1rem;
            font-size: 1.2rem;
            cursor: pointer;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15);
        }

        .sidebar-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.3);
            z-index: 150;
        }

        .sidebar-overlay.show {
            display: block;
        }

        @media (max-width: 1024px) {
            .sidebar {
                position: fixed;
                transform: translateX(-100%);
                width: 280px;
                height: 100vh;
                border-radius: 0 20px 20px 0;
            }
            .sidebar.open {
                transform: translateX(0);
                box-shadow: 0 0 40px rgba(0, 0, 0, 0.3);
            }
            .sidebar-toggle {
                display: block;
            }
            .main-content {
                padding: 1rem 1rem 2rem;
                max-width: 100%;
            }
            .top-bar {
                padding: 0.5rem 1rem;
                border-radius: 20px;
                margin-top: 0.5rem;
            }
            .tabs {
                border-radius: 20px;
                padding: 0.3rem;
                gap: 0.3rem;
            }
            .tab-btn {
                padding: 0.3rem 1rem;
                font-size: 0.75rem;
                flex: 1;
                text-align: center;
            }
            .filter-bar {
                flex-direction: column;
                align-items: stretch;
                padding: 0.8rem 1rem;
            }
            .filter-bar .result-count {
                margin-left: 0;
            }
        }

        @media (max-width: 640px) {
            .top-bar .greeting {
                font-size: 0.85rem;
            }
            .top-bar .actions {
                width: 100%;
            }
            .top-bar .actions .btn {
                flex: 1;
                text-align: center;
            }
            .stats-grid {
                grid-template-columns: 1fr 1fr;
            }
            .filter-bar .search-group {
                flex-direction: column;
            }
            .filter-bar .filter-group {
                width: 100%;
            }
            .filter-bar .filter-group select {
                flex: 1;
            }
            .table-header {
                flex-direction: column;
                align-items: stretch;
            }
            .table-header .actions {
                justify-content: stretch;
            }
            .table-header .actions .btn {
                flex: 1;
                text-align: center;
            }
        }

        @media (max-width: 480px) {
            .stats-grid {
                grid-template-columns: 1fr;
            }
            .modal {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>

<style>
  .page-nav {
    display: flex;
    flex-wrap: wrap;
    gap: 8px 12px;
    padding: 12px 18px;
    background: #0a1e3c;
    border-bottom: 1px solid rgba(255,255,255,0.08);
    font-family: Arial, sans-serif;
    font-size: 12px;
    margin: 0;
    position: sticky;
    top: 0;
    z-index: 50;
  }
  .page-nav strong {
    color: #ffffff;
    margin-right: 6px;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }
  .page-nav a {
    color: #dfeaff;
    text-decoration: none;
    padding: 6px 10px;
    border-radius: 999px;
    background: rgba(255,255,255,0.07);
    transition: background 0.2s ease;
  }
  .page-nav a:hover {
    background: rgba(255,255,255,0.14);
    color: #ffffff;
  }
</style>
<div class="page-nav"><strong>Pages</strong><a href="British Poly · Home.html">Admin Home</a><a href="About Us · British Poly.html">About Us · British Poly</a><a href="Admin Profile · Account.html">Admin Profile · Account</a><a href="Admissions · British Poly.html">Admissions · British Poly</a><a href="British Poly · Home.html">British Poly · Home</a><a href="Contact Us.html">Contact Us</a><a href="Finance & Bursary Control .html">Finance & Bursary Control </a><a href="Notice Board.html">Notice Board</a><a href="page10.html">page10</a><a href="page11.html">page11</a><a href="page12.html">page12</a><a href="page13.html">page13</a><a href="page14.html">page14</a><a href="page15.html">page15</a><a href="page16.html">page16</a><a href="page17.html">page17</a><a href="page18.html">page18</a><a href="page19.html">page19</a><a href="page21.html">page21</a><a href="page23.html">page23</a><a href="page7.html">page7</a><a href="page8.html">page8</a><a href="page9.html">page9</a><a href="Parent & Guardian Portal.html">Parent & Guardian Portal</a><a href="Programs · British Poly.html">Programs · British Poly</a><a href="Staff Management.html">Staff Management</a><a href="Student Life.html">Student Life</a></div>

    <!-- ===== SIDEBAR TOGGLE ===== -->
    <button class="sidebar-toggle" id="sidebarToggle">☰</button>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>

    <!-- ===== SIDEBAR ===== -->
    <aside class="sidebar" id="sidebar">
        <div class="logo">
            British<span>Poly</span>
            <small>Administration</small>
        </div>

        <div class="nav-section">
            <div class="label">Main</div>
            <a href="#" class="nav-item">
                <span class="icon">📊</span> Dashboard
            </a>
            <a href="#" class="nav-item active">
                <span class="icon">💼</span> SIWES & IT Tracker
                <span class="badge">18</span>
            </a>
            <a href="#" class="nav-item">
                <span class="icon">📝</span> Result Approval
                <span class="badge">12</span>
            </a>
            <a href="#" class="nav-item">
                <span class="icon">👨‍🎓</span> Student Registry
                <span class="badge">1,247</span>
            </a>
        </div>

        <div class="nav-section">
            <div class="label">Management</div>
            <a href="#" class="nav-item">
                <span class="icon">📚</span> Curriculum
            </a>
            <a href="#" class="nav-item">
                <span class="icon">📅</span> Sessions
            </a>
            <a href="#" class="nav-item">
                <span class="icon">💰</span> Finance
            </a>
        </div>

        <div class="nav-section">
            <div class="label">Settings</div>
            <a href="#" class="nav-item">
                <span class="icon">🔐</span> Access Control
            </a>
            <a href="#" class="nav-item">
                <span class="icon">⚙️</span> System Settings
            </a>
        </div>

        <div class="user-card">
            <div class="avatar">AD</div>
            <div class="info">
                <div class="name">Admin User</div>
                <div class="role">Administrator</div>
            </div>
        </div>
    </aside>

    <!-- ===== MAIN CONTENT ===== -->
    <div class="main-content">

        <!-- Top Bar -->
        <div class="top-bar">
            <div class="greeting">
                💼 SIWES & Industrial Training Tracker
                <small>| Manage internships &amp; placements</small>
            </div>
            <div class="actions">
                <button class="btn btn-primary" onclick="openModal('placement')">➕ Add Placement</button>
                <button class="btn btn-success" onclick="exportCSV()">📥 Export CSV</button>
                <button class="btn btn-outline" onclick="refreshData()">🔄 Refresh</button>
            </div>
        </div>

        <!-- Stats -->
        <div class="stats-grid" id="statsGrid">
            <div class="stat-card">
                <div class="number" id="totalPlacements">0</div>
                <div class="label">Total Placements</div>
            </div>
            <div class="stat-card">
                <div class="number" id="activePlacements">0</div>
                <div class="label">Active</div>
            </div>
            <div class="stat-card">
                <div class="number" id="completedPlacements">0</div>
                <div class="label">Completed</div>
            </div>
            <div class="stat-card">
                <div class="number" id="pendingPlacements">0</div>
                <div class="label">Pending Approval</div>
            </div>
        </div>

        <!-- Tabs -->
        <div class="tabs">
            <button class="tab-btn active" data-tab="tab-placements">📋 Placements</button>
            <button class="tab-btn" data-tab="tab-supervisors">👨‍🏫 Supervisors</button>
            <button class="tab-btn" data-tab="tab-evaluations">📊 Evaluations</button>
            <button class="tab-btn" data-tab="tab-companies">🏢 Companies</button>
        </div>

        <!-- ===== TAB 1: PLACEMENTS ===== -->
        <div class="tab-content active" id="tab-placements">
            <div class="filter-bar">
                <div class="search-group">
                    <input type="text" id="placementSearch" placeholder="🔎 Search by student or company...">
                </div>
                <div class="filter-group">
                    <label for="placementStatusFilter">Status</label>
                    <select id="placementStatusFilter">
                        <option value="all">All Status</option>
                        <option value="active">Active</option>
                        <option value="pending">Pending</option>
                        <option value="completed">Completed</option>
                        <option value="suspended">Suspended</option>
                    </select>
                    <label for="placementProgramFilter">Program</label>
                    <select id="placementProgramFilter">
                        <option value="all">All Programs</option>
                        <option value="Computer Science">Computer Science</option>
                        <option value="Engineering">Engineering</option>
                        <option value="Business">Business</option>
                        <option value="Health Sciences">Health Sciences</option>
                    </select>
                </div>
                <span class="result-count">Showing <strong id="placementResultCount">0</strong> placements</span>
            </div>

            <div class="table-section">
                <div class="table-header">
                    <span style="font-weight:600; font-size:0.9rem;">📋 Student Placements</span>
                    <div class="actions">
                        <button class="btn btn-sm btn-primary" onclick="openModal('placement')">➕ Add Placement</button>
                    </div>
                </div>
                <div style="overflow-x:auto;">
                    <table>
                        <thead>
                            <tr>
                                <th>Student</th>
                                <th>Student ID</th>
                                <th>Program</th>
                                <th>Company</th>
                                <th>Start Date</th>
                                <th>End Date</th>
                                <th>Status</th>
                                <th style="text-align:right;">Actions</th>
                            </tr>
                        </thead>
                        <tbody id="placementsBody">
                            <!-- dynamic -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- ===== TAB 2: SUPERVISORS ===== -->
        <div class="tab-content" id="tab-supervisors">
            <div class="filter-bar">
                <div class="search-group">
                    <input type="text" id="supervisorSearch" placeholder="🔎 Search supervisors...">
                </div>
                <div class="filter-group">
                    <label for="supervisorTypeFilter">Type</label>
                    <select id="supervisorTypeFilter">
                        <option value="all">All</option>
                        <option value="internal">Internal</option>
                        <option value="external">External</option>
                    </select>
                </div>
                <span class="result-count">Showing <strong id="supervisorResultCount">0</strong> supervisors</span>
            </div>

            <div class="table-section">
                <div class="table-header">
                    <span style="font-weight:600; font-size:0.9rem;">👨‍🏫 Supervisors</span>
                    <div class="actions">
                        <button class="btn btn-sm btn-primary" onclick="openModal('supervisor')">➕ Add Supervisor</button>
                    </div>
                </div>
                <div style="overflow-x:auto;">
                    <table>
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Department</th>
                                <th>Type</th>
                                <th>Students</th>
                                <th style="text-align:right;">Actions</th>
                            </tr>
                        </thead>
                        <tbody id="supervisorsBody">
                            <!-- dynamic -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- ===== TAB 3: EVALUATIONS ===== -->
        <div class="tab-content" id="tab-evaluations">
            <div class="filter-bar">
                <div class="search-group">
                    <input type="text" id="evalSearch" placeholder="🔎 Search evaluations...">
                </div>
                <div class="filter-group">
                    <label for="evalStatusFilter">Status</label>
                    <select id="evalStatusFilter">
                        <option value="all">All</option>
                        <option value="approved">Approved</option>
                        <option value="review">Under Review</option>
                        <option value="pending">Pending</option>
                    </select>
                </div>
                <span class="result-count">Showing <strong id="evalResultCount">0</strong> evaluations</span>
            </div>

            <div class="table-section">
                <div class="table-header">
                    <span style="font-weight:600; font-size:0.9rem;">📊 Student Evaluations</span>
                    <div class="actions">
                        <button class="btn btn-sm btn-primary" onclick="openModal('evaluation')">➕ Add Evaluation</button>
                    </div>
                </div>
                <div style="overflow-x:auto;">
                    <table>
                        <thead>
                            <tr>
                                <th>Student</th>
                                <th>Student ID</th>
                                <th>Company</th>
                                <th>Score</th>
                                <th>Grade</th>
                                <th>Status</th>
                                <th style="text-align:right;">Actions</th>
                            </tr>
                        </thead>
                        <tbody id="evaluationsBody">
                            <!-- dynamic -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- ===== TAB 4: COMPANIES ===== -->
        <div class="tab-content" id="tab-companies">
            <div class="filter-bar">
                <div class="search-group">
                    <input type="text" id="companySearch" placeholder="🔎 Search companies...">
                </div>
                <div class="filter-group">
                    <label for="companyIndustryFilter">Industry</label>
                    <select id="companyIndustryFilter">
                        <option value="all">All Industries</option>
                        <option value="Technology">Technology</option>
                        <option value="Engineering">Engineering</option>
                        <option value="Finance">Finance</option>
                        <option value="Healthcare">Healthcare</option>
                        <option value="Education">Education</option>
                        <option value="Manufacturing">Manufacturing</option>
                    </select>
                </div>
                <span class="result-count">Showing <strong id="companyResultCount">0</strong> companies</span>
            </div>

            <div class="table-section">
                <div class="table-header">
                    <span style="font-weight:600; font-size:0.9rem;">🏢 Partner Companies</span>
                    <div class="actions">
                        <button class="btn btn-sm btn-primary" onclick="openModal('company')">➕ Add Company</button>
                    </div>
                </div>
                <div style="overflow-x:auto;">
                    <table>
                        <thead>
                            <tr>
                                <th>Company Name</th>
                                <th>Industry</th>
                                <th>Location</th>
                                <th>Website</th>
                                <th>Students Placed</th>
                                <th style="text-align:right;">Actions</th>
                            </tr>
                        </thead>
                        <tbody id="companiesBody">
                            <!-- dynamic -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </div>

    <!-- ===== MODAL ===== -->
    <div class="modal-overlay" id="mainModal">
        <div class="modal">
            <h2 id="modalTitle">Add New Item</h2>
            <div id="modalBody">
                <!-- dynamic -->
            </div>
        </div>
    </div>

    <!-- ===== TOAST ===== -->
    <div id="toast" class="toast">message</div>

    <script>
        // ===== DATA =====
        let placements = [
            { id: 1, student: 'Sarah Johnson', studentId: 'STU-2025-002', program: 'Computer Science',
                company: 'Google', startDate: '2025-06-01', endDate: '2025-08-30', status: 'active' },
            { id: 2, student: 'Michael Okafor', studentId: 'STU-2025-003', program: 'Business', company: 'Goldman Sachs',
                startDate: '2025-06-15', endDate: '2025-09-15', status: 'active' },
            { id: 3, student: 'Aisha Labana', studentId: 'STU-2025-004', program: 'Health Sciences', company: 'NHS',
                startDate: '2025-05-20', endDate: '2025-08-20', status: 'completed' },
            { id: 4, student: 'David Chen', studentId: 'STU-2025-005', program: 'Computer Science', company: 'Microsoft',
                startDate: '2025-07-01', endDate: '2025-10-01', status: 'pending' },
            { id: 5, student: 'Emma Williams', studentId: 'STU-2025-006', program: 'Computer Science', company: 'Amazon',
                startDate: '2025-06-10', endDate: '2025-09-10', status: 'active' },
            { id: 6, student: 'James Kofi', studentId: 'STU-2025-007', program: 'Engineering', company: 'Rolls-Royce',
                startDate: '2025-05-15', endDate: '2025-08-15', status: 'suspended' },
            { id: 7, student: 'Sophia Patel', studentId: 'STU-2025-008', program: 'Computer Science', company: 'Meta',
                startDate: '2025-06-20', endDate: '2025-09-20', status: 'active' },
            { id: 8, student: 'Thomas Mensah', studentId: 'STU-2025-009', program: 'Engineering', company: 'Tesla',
                startDate: '2025-07-05', endDate: '2025-10-05', status: 'pending' }
        ];

        let supervisors = [
            { id: 1, name: 'Dr. Peter Kofi', email: 'peter.kofi@britishpoly.org', department: 'Computer Science',
                type: 'internal', students: 4 },
            { id: 2, name: 'Prof. Sarah Mensah', email: 'sarah.mensah@britishpoly.org', department: 'Business',
                type: 'internal', students: 3 },
            { id: 3, name: 'Dr. Amara Abubakar', email: 'amara.abubakar@britishpoly.org', department: 'Engineering',
                type: 'internal', students: 2 },
            { id: 4, name: 'Mr. John Smith', email: 'john.smith@google.com', department: 'Technology', type: 'external',
                students: 5 },
            { id: 5, name: 'Ms. Jane Doe', email: 'jane.doe@microsoft.com', department: 'Technology', type: 'external',
                students: 3 }
        ];

        let evaluations = [
            { id: 1, student: 'Sarah Johnson', studentId: 'STU-2025-002', company: 'Google', score: 92, grade: 'A',
                status: 'approved' },
            { id: 2, student: 'Michael Okafor', studentId: 'STU-2025-003', company: 'Goldman Sachs', score: 78,
                grade: 'B', status: 'review' },
            { id: 3, student: 'Aisha Labana', studentId: 'STU-2025-004', company: 'NHS', score: 88, grade: 'A',
                status: 'approved' },
            { id: 4, student: 'David Chen', studentId: 'STU-2025-005', company: 'Microsoft', score: 65, grade: 'C',
                status: 'pending' },
            { id: 5, student: 'Emma Williams', studentId: 'STU-2025-006', company: 'Amazon', score: 74, grade: 'B',
                status: 'review' }
        ];

        let companies = [
            { id: 1, name: 'Google', industry: 'Technology', location: 'London, UK', website: 'google.com',
                studentsPlaced: 12 },
            { id: 2, name: 'Microsoft', industry: 'Technology', location: 'Reading, UK', website: 'microsoft.com',
                studentsPlaced: 8 },
            { id: 3, name: 'Goldman Sachs', industry: 'Finance', location: 'London, UK', website: 'goldmansachs.com',
                studentsPlaced: 5 },
            { id: 4, name: 'NHS', industry: 'Healthcare', location: 'London, UK', website: 'nhs.uk', studentsPlaced: 10 },
            { id: 5, name: 'Rolls-Royce', industry: 'Engineering', location: 'Derby, UK', website: 'rolls-royce.com',
                studentsPlaced: 6 },
            { id: 6, name: 'Amazon', industry: 'Technology', location: 'London, UK', website: 'amazon.co.uk',
                studentsPlaced: 7 },
            { id: 7, name: 'Tesla', industry: 'Manufacturing', location: 'Berlin, Germany', website: 'tesla.com',
                studentsPlaced: 4 },
            { id: 8, name: 'Meta', industry: 'Technology', location: 'London, UK', website: 'meta.com',
                studentsPlaced: 6 }
        ];

        let nextId = { placement: 9, supervisor: 6, evaluation: 6, company: 9 };

        // ===== DOM REFS =====
        const modal = document.getElementById('mainModal');
        const modalTitle = document.getElementById('modalTitle');
        const modalBody = document.getElementById('modalBody');
        const toast = document.getElementById('toast');

        // ===== TAB SWITCHING =====
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
                document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
                this.classList.add('active');
                document.getElementById(this.dataset.tab).classList.add('active');
                renderAll();
            });
        });

        // ===== RENDER ALL =====
        function renderAll() {
            renderPlacements();
            renderSupervisors();
            renderEvaluations();
            renderCompanies();
            updateStats();
        }

        // ===== UPDATE STATS =====
        function updateStats() {
            document.getElementById('totalPlacements').textContent = placements.length;
            document.getElementById('activePlacements').textContent = placements.filter(p => p.status === 'active').length;
            document.getElementById('completedPlacements').textContent = placements.filter(p => p.status === 'completed')
                .length;
            document.getElementById('pendingPlacements').textContent = placements.filter(p => p.status === 'pending').length;
        }

        // ===== RENDER PLACEMENTS =====
        function renderPlacements() {
            const searchTerm = document.getElementById('placementSearch').value.toLowerCase().trim();
            const status = document.getElementById('placementStatusFilter').value;
            const program = document.getElementById('placementProgramFilter').value;

            let filtered = placements.filter(p => {
                const matchSearch = p.student.toLowerCase().includes(searchTerm) ||
                    p.company.toLowerCase().includes(searchTerm);
                const matchStatus = status === 'all' || p.status === status;
                const matchProgram = program === 'all' || p.program === program;
                return matchSearch && matchStatus && matchProgram;
            });

            document.getElementById('placementResultCount').textContent = filtered.length;

            let html = '';
            filtered.forEach(p => {
                const statusClass = 'status-' + p.status;
                html += `
                    <tr>
                        <td><strong>${p.student}</strong></td>
                        <td>${p.studentId}</td>
                        <td>${p.program}</td>
                        <td>${p.company}</td>
                        <td>${formatDate(p.startDate)}</td>
                        <td>${formatDate(p.endDate)}</td>
                        <td><span class="status-badge ${statusClass}">${p.status.charAt(0).toUpperCase() + p.status.slice(1)}</span></td>
                        <td style="text-align:right;">
                            <div class="action-btns">
                                <button class="btn btn-sm btn-primary" onclick="editItem('placement', ${p.id})">✎</button>
                                <button class="btn btn-sm btn-danger" onclick="deleteItem('placement', ${p.id})">🗑️</button>
                            </div>
                        </td>
                    </tr>
                `;
            });
            document.getElementById('placementsBody').innerHTML = html || `<tr><td colspan="8" style="text-align:center; padding:1.5rem; color:#6a7f9e;">No placements found.</td></tr>`;
        }

        // ===== RENDER SUPERVISORS =====
        function renderSupervisors() {
            const searchTerm = document.getElementById('supervisorSearch').value.toLowerCase().trim();
            const type = document.getElementById('supervisorTypeFilter').value;

            let filtered = supervisors.filter(s => {
                const matchSearch = s.name.toLowerCase().includes(searchTerm) ||
                    s.email.toLowerCase().includes(searchTerm) ||
                    s.department.toLowerCase().includes(searchTerm);
                const matchType = type === 'all' || s.type === type;
                return matchSearch && matchType;
            });

            document.getElementById('supervisorResultCount').textContent = filtered.length;

            let html = '';
            filtered.forEach(s => {
                html += `
                    <tr>
                        <td><strong>${s.name}</strong></td>
                        <td>${s.email}</td>
                        <td>${s.department}</td>
                        <td><span class="status-badge status-${s.type === 'internal' ? 'active' : 'approved'}">${s.type.charAt(0).toUpperCase() + s.type.slice(1)}</span></td>
                        <td>${s.students}</td>
                        <td style="text-align:right;">
                            <div class="action-btns">
                                <button class="btn btn-sm btn-primary" onclick="editItem('supervisor', ${s.id})">✎</button>
                                <button class="btn btn-sm btn-danger" onclick="deleteItem('supervisor', ${s.id})">🗑️</button>
                            </div>
                        </td>
                    </tr>
                `;
            });
            document.getElementById('supervisorsBody').innerHTML = html || `<tr><td colspan="6" style="text-align:center; padding:1.5rem; color:#6a7f9e;">No supervisors found.</td></tr>`;
        }

        // ===== RENDER EVALUATIONS =====
        function renderEvaluations() {
            const searchTerm = document.getElementById('evalSearch').value.toLowerCase().trim();
            const status = document.getElementById('evalStatusFilter').value;

            let filtered = evaluations.filter(e => {
                const matchSearch = e.student.toLowerCase().includes(searchTerm) ||
                    e.company.toLowerCase().includes(searchTerm);
                const matchStatus = status === 'all' || e.status === status;
                return matchSearch && matchStatus;
            });

            document.getElementById('evalResultCount').textContent = filtered.length;

            let html = '';
            filtered.forEach(e => {
                const statusClass = 'status-' + e.status;
                const gradeClass = 'grade-' + e.grade;
                html += `
                    <tr>
                        <td><strong>${e.student}</strong></td>
                        <td>${e.studentId}</td>
                        <td>${e.company}</td>
                        <td>${e.score}%</td>
                        <td><span class="grade-badge ${gradeClass}">${e.grade}</span></td>
                        <td><span class="status-badge ${statusClass}">${e.status === 'review' ? 'Under Review' : e.status.charAt(0).toUpperCase() + e.status.slice(1)}</span></td>
                        <td style="text-align:right;">
                            <div class="action-btns">
                                <button class="btn btn-sm btn-primary" onclick="editItem('evaluation', ${e.id})">✎</button>
                                <button class="btn btn-sm btn-danger" onclick="deleteItem('evaluation', ${e.id})">🗑️</button>
                            </div>
                        </td>
                    </tr>
                `;
            });
            document.getElementById('evaluationsBody').innerHTML = html || `<tr><td colspan="7" style="text-align:center; padding:1.5rem; color:#6a7f9e;">No evaluations found.</td></tr>`;
        }

        // ===== RENDER COMPANIES =====
        function renderCompanies() {
            const searchTerm = document.getElementById('companySearch').value.toLowerCase().trim();
            const industry = document.getElementById('companyIndustryFilter').value;

            let filtered = companies.filter(c => {
                const matchSearch = c.name.toLowerCase().includes(searchTerm) ||
                    c.location.toLowerCase().includes(searchTerm);
                const matchIndustry = industry === 'all' || c.industry === industry;
                return matchSearch && matchIndustry;
            });

            document.getElementById('companyResultCount').textContent = filtered.length;

            let html = '';
            filtered.forEach(c => {
                html += `
                    <tr>
                        <td><strong>${c.name}</strong></td>
                        <td>${c.industry}</td>
                        <td>${c.location}</td>
                        <td><a href="https://${c.website}" target="_blank" style="color:#1a5cff;">${c.website}</a></td>
                        <td>${c.studentsPlaced}</td>
                        <td style="text-align:right;">
                            <div class="action-btns">
                                <button class="btn btn-sm btn-primary" onclick="editItem('company', ${c.id})">✎</button>
                                <button class="btn btn-sm btn-danger" onclick="deleteItem('company', ${c.id})">🗑️</button>
                            </div>
                        </td>
                    </tr>
                `;
            });
            document.getElementById('companiesBody').innerHTML = html || `<tr><td colspan="6" style="text-align:center; padding:1.5rem; color:#6a7f9e;">No companies found.</td></tr>`;
        }

        // ===== FORMAT DATE =====
        function formatDate(dateStr) {
            if (!dateStr) return 'N/A';
            const d = new Date(dateStr);
            return d.toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' });
        }

        // ===== MODAL =====
        function openModal(type, item) {
            let html = '';

            if (type === 'placement') {
                modalTitle.textContent = item ? '✎ Edit Placement' : '➕ Add Placement';
                html = `
                    <div class="form-row">
                        <div class="form-group">
                            <label>Student Name <span class="required">*</span></label>
                            <input type="text" id="plStudent" value="${item ? item.student : ''}" placeholder="John Doe">
                        </div>
                        <div class="form-group">
                            <label>Student ID <span class="required">*</span></label>
                            <input type="text" id="plStudentId" value="${item ? item.studentId : ''}" placeholder="STU-2025-001">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Program <span class="required">*</span></label>
                            <select id="plProgram">
                                <option value="Computer Science" ${item && item.program === 'Computer Science' ? 'selected' : ''}>Computer Science</option>
                                <option value="Engineering" ${item && item.program === 'Engineering' ? 'selected' : ''}>Engineering</option>
                                <option value="Business" ${item && item.program === 'Business' ? 'selected' : ''}>Business</option>
                                <option value="Health Sciences" ${item && item.program === 'Health Sciences' ? 'selected' : ''}>Health Sciences</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Company <span class="required">*</span></label>
                            <select id="plCompany">
                                ${companies.map(c => `<option value="${c.name}" ${item && item.company === c.name ? 'selected' : ''}>${c.name}</option>`).join('')}
                            </select>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Start Date <span class="required">*</span></label>
                            <input type="date" id="plStart" value="${item ? item.startDate : ''}">
                        </div>
                        <div class="form-group">
                            <label>End Date <span class="required">*</span></label>
                            <input type="date" id="plEnd" value="${item ? item.endDate : ''}">
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Status</label>
                        <select id="plStatus">
                            <option value="active" ${item && item.status === 'active' ? 'selected' : ''}>Active</option>
                            <option value="pending" ${item && item.status === 'pending' ? 'selected' : ''}>Pending</option>
                            <option value="completed" ${item && item.status === 'completed' ? 'selected' : ''}>Completed</option>
                            <option value="suspended" ${item && item.status === 'suspended' ? 'selected' : ''}>Suspended</option>
                        </select>
                    </div>
                    <div class="modal-actions">
                        <button class="btn btn-primary" onclick="saveItem('placement')">💾 Save</button>
                        <button class="btn btn-outline-blue" onclick="closeModal()">Cancel</button>
                    </div>
                `;
            } else if (type === 'supervisor') {
                modalTitle.textContent = item ? '✎ Edit Supervisor' : '➕ Add Supervisor';
                html = `
                    <div class="form-row">
                        <div class="form-group">
                            <label>Name <span class="required">*</span></label>
                            <input type="text" id="supName" value="${item ? item.name : ''}" placeholder="Dr. John Doe">
                        </div>
                        <div class="form-group">
                            <label>Email <span class="required">*</span></label>
                            <input type="email" id="supEmail" value="${item ? item.email : ''}" placeholder="john@example.com">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Department <span class="required">*</span></label>
                            <input type="text" id="supDept" value="${item ? item.department : ''}" placeholder="Computer Science">
                        </div>
                        <div class="form-group">
                            <label>Type</label>
                            <select id="supType">
                                <option value="internal" ${item && item.type === 'internal' ? 'selected' : ''}>Internal</option>
                                <option value="external" ${item && item.type === 'external' ? 'selected' : ''}>External</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Students Supervising</label>
                        <input type="number" id="supStudents" value="${item ? item.students : 0}" min="0">
                    </div>
                    <div class="modal-actions">
                        <button class="btn btn-primary" onclick="saveItem('supervisor')">💾 Save</button>
                        <button class="btn btn-outline-blue" onclick="closeModal()">Cancel</button>
                    </div>
                `;
            } else if (type === 'evaluation') {
                modalTitle.textContent = item ? '✎ Edit Evaluation' : '➕ Add Evaluation';
                html = `
                    <div class="form-row">
                        <div class="form-group">
                            <label>Student Name <span class="required">*</span></label>
                            <input type="text" id="evStudent" value="${item ? item.student : ''}" placeholder="John Doe">
                        </div>
                        <div class="form-group">
                            <label>Student ID <span class="required">*</span></label>
                            <input type="text" id="evStudentId" value="${item ? item.studentId : ''}" placeholder="STU-2025-001">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Company <span class="required">*</span></label>
                            <select id="evCompany">
                                ${companies.map(c => `<option value="${c.name}" ${item && item.company === c.name ? 'selected' : ''}>${c.name}</option>`).join('')}
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Score (%) <span class="required">*</span></label>
                            <input type="number" id="evScore" value="${item ? item.score : ''}" min="0" max="100">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Grade</label>
                            <select id="evGrade">
                                <option value="A" ${item && item.grade === 'A' ? 'selected' : ''}>A</option>
                                <option value="B" ${item && item.grade === 'B' ? 'selected' : ''}>B</option>
                                <option value="C" ${item && item.grade === 'C' ? 'selected' : ''}>C</option>
                                <option value="D" ${item && item.grade === 'D' ? 'selected' : ''}>D</option>
                                <option value="F" ${item && item.grade === 'F' ? 'selected' : ''}>F</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Status</label>
                            <select id="evStatus">
                                <option value="approved" ${item && item.status === 'approved' ? 'selected' : ''}>Approved</option>
                                <option value="review" ${item && item.status === 'review' ? 'selected' : ''}>Under Review</option>
                                <option value="pending" ${item && item.status === 'pending' ? 'selected' : ''}>Pending</option>
                            </select>
                        </div>
                    </div>
                    <div class="modal-actions">
                        <button class="btn btn-primary" onclick="saveItem('evaluation')">💾 Save</button>
                        <button class="btn btn-outline-blue" onclick="closeModal()">Cancel</button>
                    </div>
                `;
            } else if (type === 'company') {
                modalTitle.textContent = item ? '✎ Edit Company' : '➕ Add Company';
                html = `
                    <div class="form-group">
                        <label>Company Name <span class="required">*</span></label>
                        <input type="text" id="compName" value="${item ? item.name : ''}" placeholder="Google">
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Industry <span class="required">*</span></label>
                            <select id="compIndustry">
                                <option value="Technology" ${item && item.industry === 'Technology' ? 'selected' : ''}>Technology</option>
                                <option value="Engineering" ${item && item.industry === 'Engineering' ? 'selected' : ''}>Engineering</option>
                                <option value="Finance" ${item && item.industry === 'Finance' ? 'selected' : ''}>Finance</option>
                                <option value="Healthcare" ${item && item.industry === 'Healthcare' ? 'selected' : ''}>Healthcare</option>
                                <option value="Education" ${item && item.industry === 'Education' ? 'selected' : ''}>Education</option>
                                <option value="Manufacturing" ${item && item.industry === 'Manufacturing' ? 'selected' : ''}>Manufacturing</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Location <span class="required">*</span></label>
                            <input type="text" id="compLocation" value="${item ? item.location : ''}" placeholder="London, UK">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Website</label>
                            <input type="text" id="compWebsite" value="${item ? item.website : ''}" placeholder="example.com">
                        </div>
                        <div class="form-group">
                            <label>Students Placed</label>
                            <input type="number" id="compStudents" value="${item ? item.studentsPlaced : 0}" min="0">
                        </div>
                    </div>
                    <div class="modal-actions">
                        <button class="btn btn-primary" onclick="saveItem('company')">💾 Save</button>
                        <button class="btn btn-outline-blue" onclick="closeModal()">Cancel</button>
                    </div>
                `;
            }

            modalBody.innerHTML = html;
            modal.classList.add('open');
        }

        // ===== EDIT ITEM =====
        function editItem(type, id) {
            let item = null;
            if (type === 'placement') item = placements.find(p => p.id === id);
            else if (type === 'supervisor') item = supervisors.find(s => s.id === id);
            else if (type === 'evaluation') item = evaluations.find(e => e.id === id);
            else if (type === 'company') item = companies.find(c => c.id === id);
            if (item) openModal(type, item);
        }

        // ===== SAVE ITEM =====
        function saveItem(type) {
            if (type === 'placement') {
                const student = document.getElementById('plStudent').value.trim();
                const studentId = document.getElementById('plStudentId').value.trim();
                const program = document.getElementById('plProgram').value;
                const company = document.getElementById('plCompany').value;
                const startDate = document.getElementById('plStart').value;
                const endDate = document.getElementById('plEnd').value;
                const status = document.getElementById('plStatus').value;

                if (!student || !studentId || !company || !startDate || !endDate) {
                    showToast('❌ Please fill in all required fields', 'error');
                    return;
                }

                const newPlacement = { id: nextId.placement++, student, studentId, program, company, startDate, endDate,
                    status };
                placements.push(newPlacement);
                showToast('✅ Placement added successfully', 'success');
            } else if (type === 'supervisor') {
                const name = document.getElementById('supName').value.trim();
                const email = document.getElementById('supEmail').value.trim();
                const department = document.getElementById('supDept').value.trim();
                const type = document.getElementById('supType').value;
                const students = parseInt(document.getElementById('supStudents').value) || 0;

                if (!name || !email || !department) {
                    showToast('❌ Please fill in all required fields', 'error');
                    return;
                }

                const newSupervisor = { id: nextId.supervisor++, name, email, department, type, students };
                supervisors.push(newSupervisor);
                showToast('✅ Supervisor added successfully', 'success');
            } else if (type === 'evaluation') {
                const student = document.getElementById('evStudent').value.trim();
                const studentId = document.getElementById('evStudentId').value.trim();
                const company = document.getElementById('evCompany').value;
                const score = parseInt(document.getElementById('evScore').value);
                const grade = document.getElementById('evGrade').value;
                const status = document.getElementById('evStatus').value;

                if (!student || !studentId || !company || isNaN(score)) {
                    showToast('❌ Please fill in all required fields', 'error');
                    return;
                }

                const newEvaluation = { id: nextId.evaluation++, student, studentId, company, score, grade, status };
                evaluations.push(newEvaluation);
                showToast('✅ Evaluation added successfully', 'success');
            } else if (type === 'company') {
                const name = document.getElementById('compName').value.trim();
                const industry = document.getElementById('compIndustry').value;
                const location = document.getElementById('compLocation').value.trim();
                const website = document.getElementById('compWebsite').value.trim();
                const studentsPlaced = parseInt(document.getElementById('compStudents').value) || 0;

                if (!name || !location) {
                    showToast('❌ Please fill in all required fields', 'error');
                    return;
                }

                const newCompany = { id: nextId.company++, name, industry, location, website, studentsPlaced };
                companies.push(newCompany);
                showToast('✅ Company added successfully', 'success');
            }

            closeModal();
            renderAll();
        }

        // ===== DELETE ITEM =====
        function deleteItem(type, id) {
            let confirmMsg = '';
            if (type === 'placement') confirmMsg = 'Delete this placement record?';
            else if (type === 'supervisor') confirmMsg = 'Delete this supervisor?';
            else if (type === 'evaluation') confirmMsg = 'Delete this evaluation?';
            else if (type === 'company') confirmMsg = 'Delete this company?';

            if (!confirm(confirmMsg)) return;

            if (type === 'placement') placements = placements.filter(p => p.id !== id);
            else if (type === 'supervisor') supervisors = supervisors.filter(s => s.id !== id);
            else if (type === 'evaluation') evaluations = evaluations.filter(e => e.id !== id);
            else if (type === 'company') companies = companies.filter(c => c.id !== id);

            renderAll();
            showToast('🗑️ Item deleted', 'success');
        }

        // ===== CLOSE MODAL =====
        function closeModal() {
            modal.classList.remove('open');
        }

        // ===== REFRESH =====
        function refreshData() {
            renderAll();
            showToast('↻ Data refreshed', 'success');
        }

        // ===== EXPORT CSV =====
        function exportCSV() {
            const headers = ['Type', 'Student/Name', 'Company', 'Start Date', 'End Date', 'Status'];
            let rows = [];

            placements.forEach(p => {
                rows.push(['Placement', p.student, p.company, p.startDate, p.endDate, p.status]);
            });
            evaluations.forEach(e => {
                rows.push(['Evaluation', e.student, e.company, e.score, e.grade, e.status]);
            });

            let csv = headers.join(',') + '\n';
            rows.forEach(row => {
                csv += row.map(cell => `"${cell}"`).join(',') + '\n';
            });

            const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
            const link = document.createElement('a');
            const url = URL.createObjectURL(blob);
            link.setAttribute('href', url);
            link.setAttribute('download',
                `siwes_it_tracker_${new Date().toISOString().slice(0,10)}.csv`);
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
            URL.revokeObjectURL(url);

            showToast(`✅ Exported ${rows.length} records`, 'success');
        }

        // ===== TOAST =====
        let toastTimer;

        function showToast(msg, type = 'success') {
            toast.textContent = msg;
            toast.className = 'toast ' + type + ' show';
            clearTimeout(toastTimer);
            toastTimer = setTimeout(() => {
                toast.classList.remove('show');
            }, 3000);
        }

        // ===== SIDEBAR TOGGLE =====
        const sidebar = document.getElementById('sidebar');
        const overlay = document.getElementById('sidebarOverlay');
        const toggleBtn = document.getElementById('sidebarToggle');

        function toggleSidebar() {
            sidebar.classList.toggle('open');
            overlay.classList.toggle('show');
        }

        toggleBtn.addEventListener('click', toggleSidebar);
        overlay.addEventListener('click', toggleSidebar);

        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                if (sidebar.classList.contains('open')) toggleSidebar();
                if (modal.classList.contains('open')) closeModal();
            }
        });

        // ===== EVENT BINDING =====
        document.getElementById('placementSearch').addEventListener('input', renderPlacements);
        document.getElementById('placementStatusFilter').addEventListener('change', renderPlacements);
        document.getElementById('placementProgramFilter').addEventListener('change', renderPlacements);

        document.getElementById('supervisorSearch').addEventListener('input', renderSupervisors);
        document.getElementById('supervisorTypeFilter').addEventListener('change', renderSupervisors);

        document.getElementById('evalSearch').addEventListener('input', renderEvaluations);
        document.getElementById('evalStatusFilter').addEventListener('change', renderEvaluations);

        document.getElementById('companySearch').addEventListener('input', renderCompanies);
        document.getElementById('companyIndustryFilter').addEventListener('change', renderCompanies);

        // ===== INIT =====
        renderAll();
        console.log('✅ British Poly · SIWES & Industrial Training Tracker loaded');
        console.log(`💼 ${placements.length} placements, ${supervisors.length} supervisors, ${evaluations.length} evaluations, ${companies.length} companies loaded`);
    </script>

</body>
</html>